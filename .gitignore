# Rust build artifacts (workspace root + any stray per-crate target dirs)
/target
**/target/

# Harness output — regenerated every run, would create noise/conflicts
score.json

# Editor / OS noise
*.swp
.DS_Store
.idea/
.vscode/
